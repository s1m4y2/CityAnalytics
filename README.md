# CityAnalytics

## Çalýþtýrma (lokal)
1. `copy appsettings.example.json appsettings.json`
2. `appsettings.json` içindeki `Data:CsvPath` ve `ConnectionStrings:Default` deðerlerini düzenle
3. `dotnet run`

Not: `appsettings.json`, `.db` ve `.csv` dosyalarý repoya dahil edilmez.
